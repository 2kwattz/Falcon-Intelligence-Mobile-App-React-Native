# Falcon Intelligence assets

Replace the vector placeholder mark used by `LogoMark` with the official logo when it is available. Store raster images in `images/`, icons in `icons/`, and animation files in `animations/`.
